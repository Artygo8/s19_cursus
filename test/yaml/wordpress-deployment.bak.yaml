apiVersion: v1
kind: Service
metadata:
  name: wordpress
spec:
  ports:
    - port: 5050
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: wordpress
    tier: frontend
  type: LoadBalancer
  loadBalancerIP: 192.168.64.233
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: wp-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/wp"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: wordpress
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  selector:
    matchLabels:
      app: wordpress
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: wordpress
        tier: frontend
    spec:
      containers:
      - image: wordpress:agossuin
        imagePullPolicy: Never
        name: wordpress
        ports:
        - containerPort: 5050
        volumeMounts:
        - mountPath: "/var/www/localhost"
          name: wp-pv-volume
      volumes:
      - name: wp-pv-volume
        persistentVolumeClaim:
          claimName: wp-pv-claim
